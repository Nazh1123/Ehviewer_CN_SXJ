# EhViewerNz 2.0.2.3.3.13

#### 基于 [EhViewer[xiaojieonly] 2.0.2.3 ](https://github.com/xiaojieonly/Ehviewer_CN_SXJ/releases/tag/2.0.2.3)

## Fork 更新内容

### 动态 WebP 播放控制

- 新增动态 WebP 解码方式 (主要基于 libwebp)，默认关闭。
- 新增动态 WebP 播放控制，支持播放/暂停、进度与时间显示、拖动跳转、倍速播放、长按快进、播放完毕自动翻页；
- 动图播放卡顿时显示降级入口：首次降低解码分辨率，再次需要时回退解码方式；
- “从上至下”阅读模式继续使用原有解码路径。

### 图片保存
- 取消左下角“快速保存”区域，“快速翻页”模式改为仅支持左右翻页区域的快速点击翻页，中间区域现支持双击放大；
- 长按保存提示由文字条改为图标动画；
- 保存提示显示期间点击图标，可撤销本次保存操作。

### 已下载画廊管理

- 新增“下载标签连续浏览”：按下载标签顺序在同一页面连续展示画廊，也可从下载列表菜单即时切回原单标签模式。
- 连续模式增加标签标题与数量、标签重命名、标签定位、拖动排序及标签快捷搜索；超过 `50` 个画廊的标签默认折叠，并可进入独立标签页浏览。
- 快捷归类标签支持搜索映射：`A:` 对应作者、`C:` 对应角色扮演者、`U:` 对应上传者，`AI:` 与 `M:` 使用标签值直接搜索。
- 已下载画廊标签搜索改为不区分大小写，保留双空格分隔多标签的兼容方式，并支持标签名包含匹配。
- 下载标签多选菜单新增“删除空标签”，只清理所选且不含画廊的标签。
- 下载设置与 Fork 额外功能中新增“删除本地不存在的画廊”，扫描并清理下载列表中目录缺失或空目录的记录；`content://` 外部归档会被跳过。

### 订阅与界面

- 侧边栏“更新订阅”新增下一次自动检查倒计时，以 `MM:SS` 实时显示；仅在自动更新已开启且至少启用一个订阅源时出现。
- 调整动图面板、页面滑动条、快捷设置与保存提示的层级、间距和触摸处理，修复控件冲突。
- 补充动态 WebP 设置及相关提示的多语言资源。