# EhViewerNz 2.0.2.3.3.13

#### 基于 [EhViewer[xiaojieonly] 2.0.2.3 ](https://github.com/xiaojieonly/Ehviewer_CN_SXJ/releases/tag/2.0.2.3)

## Fork 更新内容

### 动态 WebP 播放控制

- 新增动态 WebP 解码方式 (主要基于 libwebp)，默认关闭。
- 新增动态 WebP 播放控制，支持播放/暂停、进度与时间显示、拖动跳转、倍速播放、长按快进、播放完毕自动翻页；
- 动图播放卡顿时显示警告，可点击作为降级入口：首次降低解码分辨率，再次回退解码方式；
- “从上至下”阅读模式继续使用原有解码路径。

### 快速保存
- 取消左下角“快速保存”区域，“快速翻页”模式改为仅支持左右翻页区域的快速点击翻页，中间区域现支持双击放大；
- 保存成功后显示图标，可通过点击该图标撤销本次保存操作。

### 已下载画廊管理

- “下载标签连续浏览”，可在同一页面连续展示不同下载标签内的画廊；
  - 可通过下载列表菜单中的“切换列表模式”随时切换；
  - 标签标题显示画廊数量；点击标题可重命名标签，长按标签可直接发起对应画廊搜索；
- 下载列表界面右下角新增按钮“归类收纳”，可通过多选已下载画廊，根据关键词（作者、Coser、Group、标题、上传者）进行分类并放入对应下载标签，标签支持 `#注释`；
- 已下载画廊搜索改为不区分大小写；
- 下载标签管理新增功能：多选、区间选择、批量删除、批量移动、排序、删除空标签；
- 新增“删除本地不存在画廊”，扫描并清理下载列表中目录缺失或空目录的记录。

### 杂项

- 侧边栏提供过量滑动以匹配单手操作情况；
- 侧边栏“更新订阅”新增下一次自动检查倒计时；
- 订阅系统支持复用自动更新缓存；
- 画廊浏览页中，画廊多选方式从长按改为右下角按钮；
- 缩略图模式可在封面右下角显示已下载标识；
- 画廊左滑跳转预览现在支持根据阅读情况直接跳转第二页；
- UI和文本重构。