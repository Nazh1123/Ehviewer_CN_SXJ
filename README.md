# EhViewerNz

EhViewerNz 是基于 [Ehviewer_CN_SXJ](https://github.com/xiaojieonly/Ehviewer_CN_SXJ) 的 Fork.

## Fork 功能

### 功能与设置

- 设置菜单中为Fork各项功能加入自定义和开关设置。
- `设置 - Fork 额外功能` 集中提供对应同步设置，以供快速浏览。
- Fork 功能包含：
  - [快速翻页](#图片浏览)、[长按保存](#图片浏览)、[自动翻页](#图片浏览)、[快捷方式栏](#图片浏览)、[动态 WebP 控制](#图片浏览)；
  - [书签订阅](#订阅聚合)、[自动获取订阅更新](#订阅聚合)、[更新倒计时](#订阅聚合)；
  - [画廊多选](#画廊浏览列表)、[搜索语言项](#画廊浏览列表)、[下载标识](#画廊浏览列表)；
  - [左滑预览](#画廊详情与预览)、[预览UI缩放](#画廊详情与预览)、[预览快速跳转](#画廊详情与预览)；
  - [提取作者](#画廊详情与预览)、[父画廊浏览](#画廊详情与预览)、[本地打开画廊](#画廊详情与预览)；
  - [下载画廊归类](#已下载画廊管理)、[下载标签连续浏览](#已下载画廊管理)、[下载标签管理](#已下载画廊管理)、[无效记录清理](#已下载画廊管理)。

### 图片浏览

- “快速翻页”，开启后左右区域将支持快速单击翻页，其余区域仍支持双击放大。
- “长按保存”，用于快速收藏单张图片。长按翻页区域即可保存当前图片，其位置随阅读方向变化。
- “保存后翻页”，开启后长按保存后将自动翻至下一页。
- “手动保存路径”，用于单独设定长按保存的目标目录。
- 长按保存成功后以图标提示；在提示消失前点击图标可撤销本次保存。连续保存时会以动画反馈，保存上一页时会显示角标。
- “自动翻页”改为使用毫秒值，支持输入 `20 ~ 30000 ms`，或通过滑动条设置 `100 ~ 15000 ms`，默认 `2000 ms`。
- “快捷方式栏”，点击中部下方区域，与下方滑动条一起，在屏幕上方出现。包含多个以上设定在内的阅读相关快捷按钮。
- “动图控制”默认开启，为动态 WebP 提供播放/暂停、播放进度、时间显示、倍速与顺序播放功能。该功能用于非“从上至下”阅读模式；不适用时自动使用原有解码方式。
  - 在屏幕底部 `25%` 区域滑动可跳转播放进度；双击该区域可切换播放与暂停。
  - 唤出页面滑动条后，可使用播放/暂停、`0.5x / 1x / 1.5x / 2x` 倍速和单图循环/顺序播放按钮。
  - “长按倍速”支持 `0.1x ~ 3.0x`，默认 `2.0x`。长按屏幕中下区域时临时以该速度播放，松开后恢复；设为 `1.0x` 时长按仍用于唤出菜单。
  - 开启“顺序播放”后，动图结束时自动进入下一页；持续长按时，倍速状态可随顺序播放延续到下一页。
  - 播放卡顿时会显示警告按钮，点击后降低当前页解码开销；如仍有需要则回退到系统解码。
  - “自动翻页按钮”可控制在页面滑动条出现时是否同时显示原有的自动翻页按钮。

### 订阅聚合

- “书签订阅”，合并所有已启用订阅的书签搜索结果，按 GID 去重后以发布时间排序。
- 在书签栏设置或 `设置 - Fork 额外功能 - 书签订阅` 中管理订阅。
- “全局订阅”，在书签订阅基础上额外合并 EH 网页自带订阅内容。
- “自动获取订阅更新”，自动获取未读画廊数量并在侧边栏中显示。
  - 可选择是否检查 EH 订阅或书签订阅；
  - 自动每小时检查一次，也可通过侧边栏“更新订阅”立即检查。
- 打开侧边栏时，“更新订阅”旁会以 `MM:SS` 显示距离下一次自动检查的时间；关闭自动更新或未选择任何订阅源时不显示。
- 订阅页内长按画廊搜索其来源书签。
- 启动页支持“书签订阅”或“全局订阅”。

### 画廊浏览列表

- 侧边栏提供“搜索语言项”按钮，可当前搜索条件中添加或删除语言标签并进行搜索，可在设置更改不同语言标签。
- 缩略图模式可在封面右下角显示已下载标识。
- 画廊列表右下角按键提供“画廊多选”模式，可批量下载或删除选中画廊。

### 画廊详情与预览

- “画廊预览立即跳转”，开启后详情页底部的预览入口将直接跳转到第二页的新内容。
- 详情页支持向左滑动快速进入画廊预览，并根据情况决定是否直接跳转至第二页。
- 画廊预览支持双指缩放，即时调整每行图片（缩略图）数量，最多 `8` 张。
- “作者”按钮，优先搜索第一个 `artist:` 或 `cosplayer:` 标签，不存在时尝试从标题括号内容提取关键词进行搜索。
- 右上角菜单支持第三方软件打开画廊图片和复制画廊本地路径。
- 浏览父画廊时直接一键获取所有父画廊，并标识已下载的父画廊。

### 已下载画廊管理

- 下载列表界面右下角新增按钮“归类收纳”，可通过多选已下载画廊，根据作者关键词进行分类并放入对应下载标签，标签支持`#注释`。
- 下载标签管理新增功能：多选、批量删除、批量移动、排序。
- “下载标签连续浏览”可在同一页面中按标签顺序展示所有已下载画廊，并可通过下载列表菜单中的“切换列表模式”随时切换。
  - 标签标题显示画廊数量；点击标题可重命名标签，长按受支持的归类标签可直接发起对应画廊搜索。
  - 单个标签超过 `50` 个画廊时默认折叠，点击折叠提示进入该标签的独立列表。
  - 连续模式仍支持筛选、排序、搜索、多选与拖动；下载画廊标签搜索改为不区分大小写，并支持标签名包含匹配。
- 下载标签多选状态下可执行“删除空标签”，只删除所选且不包含画廊的标签。
- `设置 - 下载 - 删除本地不存在的画廊` 可扫描下载目录，并从下载列表清理本地目录缺失或仅含元数据文件的记录；执行前会要求确认，外部 `content://` 归档不会被误判为缺失。

